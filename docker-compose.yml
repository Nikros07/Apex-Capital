services:
  apex:
    build: .
    image: apex-capital:latest
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DB_PATH=/data/apex.db
    volumes:
      - ./data:/data
